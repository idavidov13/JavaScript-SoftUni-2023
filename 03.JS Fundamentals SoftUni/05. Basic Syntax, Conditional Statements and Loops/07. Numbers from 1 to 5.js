// Write a function that prints all the numbers from 1 to 5 (inclusive) each on a separate line.
function print() {
  let num = 1;
  while (num <= 5) {
    console.log(num);
    num++;
  }
}
print();
